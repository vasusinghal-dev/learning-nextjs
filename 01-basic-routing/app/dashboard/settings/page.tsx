import type { Metadata } from "next";

export const metadata: Metadata = {
  title: "Settings",
  description: "Configure account preferences, security, and notifications",
};

const Settings = () => {
  return <h1>Settings</h1>;
};
export default Settings;
